apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: helloworld-app
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - cluster: remote
        url: https://dev-aphid-ag7ava9s.hcp.brazilsouth.azmk8s.io:443
        environment: dev
        namespace: helloworld
      - cluster: primary
        url: https://prod-weevil-jf8xyfdc.hcp.brazilsouth.azmk8s.io:443
        environment: dev
        namespace: helloworld
  template:
    metadata:
      name: "{{cluster}}-helloworld-app"
    spec:
      project: default
      source:
        repoURL: https://github.com/zhangchl007/argo-istio-manifests.git
        targetRevision: HEAD
        path: argo-istio/helm-helloworld
        helm:
          valueFiles:
            - values/env/{{environment}}/{{cluster}}/values.yaml
            - values/env/{{environment}}/common/version.yaml
          parameters:
            - name: fullnameOverride
              value: helloworld
      destination:
        server: '{{url}}'
        namespace: "{{namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true